import { Unit } from "../src/game/unit";
var assert = require('assert');

describe('bot_tests', function() {
  describe('moveRight()', function() {
    it('should move the bot right 1 space.', function() {
        var u = new Unit();
    })
  })
})
